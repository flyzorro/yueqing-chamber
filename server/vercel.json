{
  "version": 2,
  "outputDirectory": "dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 10
    }
  },
  "build": {
    "env": {
      "PRISMA_GENERATE": "true"
    }
  },
  "buildCommand": "npm run build && npx prisma migrate deploy"
}
