## Source Request
最小 full-stack 交付：通讯录仅登录会员可见。后端要求 GET /api/members、GET /api/members/:id、GET /api/members/:id/details 未登录返回 401；移动端 members 页面未登录时不得暴露目录数据，必须显式处理 401 并跳转登录或提示先登录；必须基于真实 flyzorro/main 和真实验证，不允许 mock/stub。

## Scoped Brief
交付本次最小 full-stack 闭环，只覆盖已明确提出且已在仓库中验证存在的成员目录读取链路：
- 保护现有 GET /api/members，未登录返回 401。
- 保护现有 GET /api/members/:id，未登录返回 401。
- 保护现有 GET /api/members/:id/details，未登录返回 401。
- 更新现有移动端 members 页面及其现有认证/API 调用链路，确保未登录时不展示会员目录数据。
- 移动端必须对 401 做显式处理，采用“跳转登录”或“明确提示先登录”中的最小现有可行路径。
- 真实验收应覆盖后端受保护接口和移动端未登录访问 members 场景，不以 mock/stub 作为完成依据。
- 建议下一步为 Setup。
- execution_shape 明确为 full-stack。
- change_budget 仅允许触达现有 server/ 与 mobile/ 目录中的既有实现/测试/必要认证接线，不扩到 schema/db/crawler。

## Unknowns
- 移动端未登录 UX 应优先“自动跳转登录”还是“留在 members 页内给出明确先登录提示”；源请求允许二选一，但当前尚未确认哪个是最小且与现有导航最一致的路径。
- 现有 `member-detail` 路由是否在未登录场景下也会直接读取受保护会员数据，并因此需要纳入同一实现/QA 闭环以避免侧漏；需在 Setup/实现时基于真实调用链确认。
- 本仓库当前可稳定运行的移动端真实验证路径是自动化测试、端到端流程、还是以本地真实登录/API 联调为主；需在 Setup 阶段确认基线。

## Leader Assumptions
- `server/src/routes/members.ts` 是这三个目标 GET 接口的当前真实路由入口，因为仓库中存在该文件且 `server/src/routes/index.ts` 将 members router 挂载到 `/members`。
- `mobile/app/members.tsx` 是当前会员目录页面的真实入口，因为文件内直接请求 `API.MEMBERS` 并渲染会员列表。
- `mobile/app/utils/api.ts` 与 `mobile/app/utils/auth.ts` 是移动端现有认证头/接口接线候选位置，可用于最小化实现 401 显式处理与登录态接线，但是否都需要修改仍待 Setup 后确认。
- 本次不需要新增后端 endpoint、数据库 schema、或新的移动端页面；若真实实现发现必须扩 scope，应先回到领导/人类决策，而不是静默扩展。

## Out of Scope
- POST /api/members、PUT /api/members/:id、DELETE /api/members/:id 的权限/行为调整。
- 新增 RBAC/角色分级/字段脱敏/更细粒度 ACL。
- schema、db migration、crawler、后台管理能力、目录搜索策略重构。
- 与 members 登录可见性无直接关系的 UI 美化、重构、文档补全、接口重命名。
- 任何 mock/stub 驱动的“伪完成”验证。

## Risks/Blockers
- 现有移动端 members 页面当前直接 `fetch(API.MEMBERS)`，未走统一鉴权 helper；实现 401 显式处理时可能需要补最小认证接线，而不仅是页面文案修改。
- 现有服务端 members 测试明显覆盖未鉴权成功路径；改为受保护后，需要同步修正真实测试基线，避免把旧开放行为误当回归。
- 真实登录/令牌获取方式尚未在本任务中验证，Setup 若无法确认可用认证基线，会直接影响“真实验证”闭环。

## Recommended Next Step
Setup：基于真实 flyzorro/main 确认远端基线、建立 detached worktree、发现可运行验证命令，并为后续并行实现拆成 backend lane + mobile lane；下游拓扑保持模板定义，不在本步骤自行物化 blocked downstream tasks。

## FEATURE_SCOPE
{
  "source_request": "最小 full-stack 交付：通讯录仅登录会员可见。后端要求 GET /api/members、GET /api/members/:id、GET /api/members/:id/details 未登录返回 401；移动端 members 页面未登录时不得暴露目录数据，必须显式处理 401 并跳转登录或提示先登录；必须基于真实 flyzorro/main 和真实验证，不允许 mock/stub。",
  "scoped_brief": "交付本次最小 full-stack 闭环，只覆盖已明确提出且已在仓库中验证存在的成员目录读取链路：\n- 保护现有 GET /api/members，未登录返回 401。\n- 保护现有 GET /api/members/:id，未登录返回 401。\n- 保护现有 GET /api/members/:id/details，未登录返回 401。\n- 更新现有移动端 members 页面及其现有认证/API 调用链路，确保未登录时不展示会员目录数据。\n- 移动端必须对 401 做显式处理，采用“跳转登录”或“明确提示先登录”中的最小现有可行路径。\n- 真实验收应覆盖后端受保护接口和移动端未登录访问 members 场景，不以 mock/stub 作为完成依据。\n- 建议下一步为 Setup。\n- execution_shape 明确为 full-stack。\n- change_budget 仅允许触达现有 server/ 与 mobile/ 目录中的既有实现/测试/必要认证接线，不扩到 schema/db/crawler。",
  "in_scope": [
    "protect existing GET /api/members with 401 when unauthenticated",
    "protect existing GET /api/members/:id with 401 when unauthenticated",
    "protect existing GET /api/members/:id/details with 401 when unauthenticated",
    "update existing mobile members screen so unauthenticated users do not see member directory data",
    "explicitly handle backend 401 in the existing mobile members access flow"
  ],
  "unknowns": [
    "移动端未登录 UX 应优先“自动跳转登录”还是“留在 members 页内给出明确先登录提示”；源请求允许二选一，但当前尚未确认哪个是最小且与现有导航最一致的路径。",
    "现有 `member-detail` 路由是否在未登录场景下也会直接读取受保护会员数据，并因此需要纳入同一实现/QA 闭环以避免侧漏；需在 Setup/实现时基于真实调用链确认。",
    "本仓库当前可稳定运行的移动端真实验证路径是自动化测试、端到端流程、还是以本地真实登录/API 联调为主；需在 Setup 阶段确认基线。"
  ],
  "leader_assumptions": [
    "`server/src/routes/members.ts` 是这三个目标 GET 接口的当前真实路由入口，因为仓库中存在该文件且 `server/src/routes/index.ts` 将 members router 挂载到 `/members`。",
    "`mobile/app/members.tsx` 是当前会员目录页面的真实入口，因为文件内直接请求 `API.MEMBERS` 并渲染会员列表。",
    "`mobile/app/utils/api.ts` 与 `mobile/app/utils/auth.ts` 是移动端现有认证头/接口接线候选位置，可用于最小化实现 401 显式处理与登录态接线，但是否都需要修改仍待 Setup 后确认。",
    "本次不需要新增后端 endpoint、数据库 schema、或新的移动端页面；若真实实现发现必须扩 scope，应先回到领导/人类决策，而不是静默扩展。"
  ],
  "out_of_scope": [
    "POST /api/members、PUT /api/members/:id、DELETE /api/members/:id 的权限/行为调整。",
    "新增 RBAC/角色分级/字段脱敏/更细粒度 ACL。",
    "schema、db migration、crawler、后台管理能力、目录搜索策略重构。",
    "与 members 登录可见性无直接关系的 UI 美化、重构、文档补全、接口重命名。",
    "任何 mock/stub 驱动的“伪完成”验证。"
  ],
  "risks_blockers": [
    "现有移动端 members 页面当前直接 `fetch(API.MEMBERS)`，未走统一鉴权 helper；实现 401 显式处理时可能需要补最小认证接线，而不仅是页面文案修改。",
    "现有服务端 members 测试明显覆盖未鉴权成功路径；改为受保护后，需要同步修正真实测试基线，避免把旧开放行为误当回归。",
    "真实登录/令牌获取方式尚未在本任务中验证，Setup 若无法确认可用认证基线，会直接影响“真实验证”闭环。"
  ],
  "recommended_next_step": "Setup：基于真实 flyzorro/main 确认远端基线、建立 detached worktree、发现可运行验证命令，并为后续并行实现拆成 backend lane + mobile lane；下游拓扑保持模板定义，不在本步骤自行物化 blocked downstream tasks。",
  "execution_shape": "full-stack",
  "change_budget": {
    "allowed_layers": [
      "backend",
      "api",
      "mobile-ui",
      "auth"
    ],
    "allowed_operations": [
      "edit-existing",
      "edit-config"
    ],
    "allowed_roots": [
      "server/",
      "mobile/"
    ],
    "forbidden_layers": [
      "web-ui",
      "schema",
      "db",
      "crawler"
    ]
  },
  "initial_targets": [
    {
      "kind": "api-handler",
      "path": "server/src/routes/members.ts",
      "exists": true,
      "evidence": [
        "rg hit: server/src/routes/members.ts contains GET /api/members, GET /api/members/:id, and GET /api/members/:id/details",
        "server/src/routes/index.ts imports members router and mounts it at /members"
      ]
    },
    {
      "kind": "backend-test",
      "path": "server/src/__tests__/members.test.ts",
      "exists": true,
      "evidence": [
        "rg hit: server/src/__tests__/members.test.ts",
        "file contains route tests for GET /api/members, GET /api/members/:id, and GET /api/members/:id/details"
      ]
    },
    {
      "kind": "mobile-screen",
      "path": "mobile/app/members.tsx",
      "exists": true,
      "evidence": [
        "rg hit: mobile/app/members.tsx",
        "file fetches API.MEMBERS and renders member list UI"
      ]
    },
    {
      "kind": "mobile-api-config",
      "path": "mobile/app/utils/api.ts",
      "exists": true,
      "evidence": [
        "rg hit: mobile/app/utils/api.ts",
        "file defines API.MEMBERS and a fetchApi helper that attaches Authorization from AsyncStorage"
      ]
    },
    {
      "kind": "mobile-auth-helper",
      "path": "mobile/app/utils/auth.ts",
      "exists": true,
      "evidence": [
        "rg hit: mobile/app/utils/auth.ts",
        "file defines getToken, isLoggedIn, clearAuthData, and getAuthHeaders"
      ]
    }
  ]
}
