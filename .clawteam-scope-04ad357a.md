## Source Request
Implement the real feature documented in docs/feature-service-category-entries.md for yueqing-chamber. On the public services page, add and display the missing service category entries, unify category naming, unify category display order, and keep the existing page style with only minimal adjustments. Required categories: 金融服务, 文旅, IT 技术, 政务服务, HR 服务, 外包服务, 信息发布. Do not add booking/application flows, backend config, detail pages, complex APIs, or database migrations. Acceptance: the services page shows all required categories with consistent naming/order/style, existing entries still work, typecheck passes, and the minimum page-relevant validation passes with real evidence.

## Scoped Brief
Minimal UI-only delivery on the existing public services page: update the existing service-category data/rendering so the page shows all required categories with consistent naming, consistent display order, and current page style preserved with only minimal page-level adjustments if needed.

## Unknowns
- The exact current acceptance surface for the public services page still needs setup-stage confirmation against the running app/build flow.
- The exact page-relevant validation command(s) are not yet confirmed from project tooling beyond the existence of TypeScript and current app files.
- Whether any category naming/order source exists outside mobile/app/services.tsx is not yet confirmed.

## Leader Assumptions
- Provisional assumption: the feature is implemented in the Expo app route at mobile/app/services.tsx because workspace search shows that route plus a home-screen link to /services.
- Provisional assumption: this is a ui-only change because the request and feature doc explicitly exclude booking/application flows, backend config, complex APIs, and database migrations.
- Provisional assumption: editing existing page data and page-local presentation is sufficient unless setup proves there is a separate shared constant/component source for service categories.

## Out of Scope
- Booking, application, consultation, or any other service workflow after clicking a category.
- Backend config, backend modules, APIs, schema changes, database changes, or migrations.
- New detail pages, forms, tabs, routing trees, or admin/configuration interfaces.
- Product expansion beyond the required category presence, naming, ordering, and minimal style consistency on the existing services page.

## Risks/Blockers
- The current services screen already contains the required category labels in workspace search results, so the actual gap may be naming/order/style consistency or route-surface mismatch rather than pure missing entries.
- Validation surface is thin in the current repo snapshot (package.json exposes only Playwright scripts while the task requires typecheck), so setup must confirm the real minimum page-relevant validation path before implementation starts.
- If category definitions are duplicated elsewhere, implementation may need a small consolidation step within allowed UI roots only.

## Recommended Next Step
Setup: confirm repo validation commands, confirm the actual services-page route/render path, and capture detached-worktree/baseline evidence before splitting implementation into two scoped UI lanes and two scoped QA lanes.

## FEATURE_SCOPE
{
  "source_request": "Implement the real feature documented in docs/feature-service-category-entries.md for yueqing-chamber. On the public services page, add and display the missing service category entries, unify category naming, unify category display order, and keep the existing page style with only minimal adjustments. Required categories: 金融服务, 文旅, IT 技术, 政务服务, HR 服务, 外包服务, 信息发布. Do not add booking/application flows, backend config, detail pages, complex APIs, or database migrations. Acceptance: the services page shows all required categories with consistent naming/order/style, existing entries still work, typecheck passes, and the minimum page-relevant validation passes with real evidence.",
  "scoped_brief": "Minimal UI-only delivery on the existing public services page: update the existing service-category data/rendering so the page shows all required categories with consistent naming, consistent display order, and current page style preserved with only minimal page-level adjustments if needed.",
  "in_scope": [
    "Confirm the existing services page route and candidate source files that drive category display.",
    "Edit existing services-page UI data/rendering to ensure all required categories are displayed.",
    "Unify service category naming and display order on the existing page.",
    "Make only minimal page-relevant style adjustments needed to keep category cards visually consistent.",
    "Run typecheck and minimum page-relevant validation with real evidence in later workflow stages."
  ],
  "unknowns": [
    "The exact current acceptance surface for the public services page still needs setup-stage confirmation against the running app/build flow.",
    "The exact page-relevant validation command(s) are not yet confirmed from project tooling beyond the existence of TypeScript and current app files.",
    "Whether any category naming/order source exists outside mobile/app/services.tsx is not yet confirmed."
  ],
  "leader_assumptions": [
    "Provisional assumption: the feature is implemented in the Expo app route at mobile/app/services.tsx because workspace search shows that route plus a home-screen link to /services.",
    "Provisional assumption: this is a ui-only change because the request and feature doc explicitly exclude booking/application flows, backend config, complex APIs, and database migrations.",
    "Provisional assumption: editing existing page data and page-local presentation is sufficient unless setup proves there is a separate shared constant/component source for service categories."
  ],
  "out_of_scope": [
    "Booking, application, consultation, or any other service workflow after clicking a category.",
    "Backend config, backend modules, APIs, schema changes, database changes, or migrations.",
    "New detail pages, forms, tabs, routing trees, or admin/configuration interfaces.",
    "Product expansion beyond the required category presence, naming, ordering, and minimal style consistency on the existing services page."
  ],
  "risks_blockers": [
    "The current services screen already contains the required category labels in workspace search results, so the actual gap may be naming/order/style consistency or route-surface mismatch rather than pure missing entries.",
    "Validation surface is thin in the current repo snapshot (package.json exposes only Playwright scripts while the task requires typecheck), so setup must confirm the real minimum page-relevant validation path before implementation starts.",
    "If category definitions are duplicated elsewhere, implementation may need a small consolidation step within allowed UI roots only."
  ],
  "recommended_next_step": "Setup: confirm repo validation commands, confirm the actual services-page route/render path, and capture detached-worktree/baseline evidence before splitting implementation into two scoped UI lanes and two scoped QA lanes.",
  "execution_shape": "ui-only",
  "change_budget": {
    "allowed_layers": ["mobile-ui"],
    "allowed_operations": ["edit-existing", "add-ui-component", "add-ui-style-file"],
    "allowed_roots": ["mobile/", "docs/"],
    "forbidden_layers": ["web-ui", "backend", "api", "schema", "db", "crawler", "auth"]
  },
  "initial_targets": [
    {
      "kind": "mobile-screen",
      "path": "mobile/app/services.tsx",
      "exists": true,
      "evidence": [
        "read hit: mobile/app/services.tsx exports ServicesScreen with a local services array",
        "rg hit: ./mobile/app/services.tsx contains required category names including 金融服务, 文旅, IT 技术, 政务服务, HR 服务, 外包服务, 信息发布"
      ]
    },
    {
      "kind": "mobile-entry-link",
      "path": "mobile/app/index.tsx",
      "exists": true,
      "evidence": [
        "read hit: mobile/app/index.tsx pushes router.push('/services') from the home quick-entry card",
        "rg hit: ./mobile/app/index.tsx references '/services'"
      ]
    },
    {
      "kind": "feature-doc",
      "path": "docs/feature-service-category-entries.md",
      "exists": true,
      "evidence": [
        "read hit: docs/feature-service-category-entries.md defines required categories and out-of-scope rules",
        "rg hit: ./docs/feature-service-category-entries.md lines 18-25 list the seven required categories"
      ]
    }
  ]
}
